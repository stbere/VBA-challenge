Sub alphabetical_testing():

    'Create a script that loops through all the stocks for one year and outputs the following information
        'Dim WS_Count As Integer
         '   Dim ws As Worksheet
        Dim LastRow As Long
        'Set WS_Count equal to the number of worksheets in the active workbook
                'WS_Count = ActiveWorkbook.Worksheets.Count
    

        'Determine Last Row of Each Worksheet
            LastRow = ws.Cells(Rows.Count, 1).End(xlUp).Row
            
        
        'Begin the loop
        For i = 1 To 100
            Cells(a1).Value = "ticker"
            Cells(c1, f1).Value = "yearly change"
            Cells().Value = "percentage change"
            
        Next i
        
    'Ticker Symbol
    
    'Yearly change from the opening price at beginning of a given year to the closing price at end of that year
    
    'Percentage change from opening price at beginning of a given year to closing price at end of that year
    
    'Total stock volume of the stock
    
    'Use conditional formatting that highlights positive change in green and negative change in red

End Sub
